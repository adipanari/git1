function isOdd(number) {
    if (number % 2 != 0) {
      console.log(`${number} is ODD`)
    } else {
      console.log(`${number} is NOT ODD`)
    }
  }
  
  isOdd(10)
  isOdd(11)

 function add(p1, p2) {
    console.log(`${p1} + ${p2} = ${p1 + p2}`)
  }
  add(10, 4) 
  
  function multi(p1, p2) {
    console.log(`${p1} * ${p2} = ${p1 * p2}`)
  }
  
  multi(10, 4)  

function square(a1){
    console.log(`${a1} * ${a1} = ${a1 * a1}`)}
    square(5)
function cube(a1){
    console.log(`${a1} * ${a1} * ${a1} = ${a1 * a1 * a1}`)}
    cube(5)
